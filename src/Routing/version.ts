/**
 * Version of the routing sub-system, reported to telemetry so the team can see
 * which iteration of the feature is in use.
 *
 * Bumped when the routing module's public behavior changes, independently of
 * the SDK version.
 */
export const ROUTING_VERSION = "1.0.0";
